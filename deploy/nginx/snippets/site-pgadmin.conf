# pgadmin.canery.in — the only route to Postgres from outside this machine.
# The database itself publishes no port; this and an SSH tunnel are it.

client_max_body_size 25m;

location / {
    set $upstream http://pgadmin:80;
    proxy_pass $upstream;
    include /etc/nginx/snippets/proxy-headers.conf;
}
