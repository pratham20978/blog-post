# Let's Encrypt's HTTP-01 challenge. certbot writes its proof into a volume
# shared with this container, and it has to stay reachable over plain HTTP on
# every name — including after the redirect to HTTPS goes up, or renewal fails
# ninety days from now with nobody watching.

location /.well-known/acme-challenge/ {
    root /var/www/certbot;
}
