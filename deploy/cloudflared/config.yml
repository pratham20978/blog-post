# Generated by scripts/tunnel-setup.sh. Re-run it rather than editing.
#
# Every hostname lands on nginx, which does all the routing by Host header
# exactly as it did before — the tunnel delivers traffic, it does not route.
# http, not https: that hop never leaves the Docker network, and the tunnel
# has already encrypted everything that goes out to Cloudflare.
tunnel: b7936329-cf57-4bd4-b94e-e8febc785350
credentials-file: /etc/cloudflared/creds/b7936329-cf57-4bd4-b94e-e8febc785350.json
ingress:
  - hostname: canery.in
    service: http://nginx:80
  - hostname: www.canery.in
    service: http://nginx:80
  - hostname: api.canery.in
    service: http://nginx:80
  - hostname: pgadmin.canery.in
    service: http://nginx:80
  - hostname: minioview.canery.in
    service: http://nginx:80
  - hostname: minio.canery.in
    service: http://nginx:80
  - hostname: portainer.canery.in
    service: http://nginx:80
  # Required last rule: anything not named above is refused rather than
  # quietly handed to whatever happens to answer.
  - service: http_status:404
