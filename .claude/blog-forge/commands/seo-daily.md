---
description: Run Canery's dated daily SEO report
---

From the repository root run:

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/canery-seo" daily --config seo.config.json
```

Report the dated Markdown/JSON paths and summarize any findings. This command records observations only; it never rewrites or publishes an article.
