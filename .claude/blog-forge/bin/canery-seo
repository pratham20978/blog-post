#!/usr/bin/env python3
"""Stable executable entry point for the plugin SEO audit."""

from __future__ import annotations

import pathlib
import runpy

runpy.run_path(
    str(pathlib.Path(__file__).resolve().parents[1] / "scripts" / "seo_audit.py"),
    run_name="__main__",
)
